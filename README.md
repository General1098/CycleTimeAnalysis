# Cycle Time Tracker with Timepiece (v8)

**New in v8**
- `aggregationType` dropdown (default: `average`) — always included in payload.
- Presets + Safe Mode.
- Auth mode toggle: Header or Query.
- CSV→JSON fallback and clearer errors.

## Run
```bash
pip install -r requirements.txt
streamlit run app.py
```
